@using StoreModels;
@model List<OrderHistoryModel>
@{
    ViewData["Title"] = "Your Order History";
}

<div class="text-center">
    @if (Model != null)
    {
        <h3>@Html.DisplayFor(Model => Model.FirstOrDefault().FirstName)'s Order History</h3>
    }
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(Model => Model.FirstOrDefault().LocationName)
            </th>
            <th>
                @Html.DisplayNameFor(Model => Model.FirstOrDefault().Total)
            </th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelitem => item.LocationName)
                </td>
                <td>
                    $@Html.DisplayFor(modelitem => item.Total)
                 </td>
                <td>
                    @Html.ActionLink("Details", "DetailsOfOrder", new { orderId = item.Id, email = Model.FirstOrDefault().Email })
                </td>
            </tr>
            }
        }
        else
        {
            <tr><td>There are no orders. </td></tr>
        }

    </tbody>
</table>